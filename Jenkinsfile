#!/usr/bin/env groovy

def branch = env.BRANCH_NAME
def dockerRegistry = ''
def dockerBaseTag = "${dockerRegistry}"
def dockerImageName = 'nameeelectricboogaloo'
    stages {
        stage('Build') { 
            steps {
                // 
            }
        }
        stage('Test') { 
            steps {
                // 
            }
        }
        stage('Deploy') { 
            steps {
                // 
            }
        }
    }
